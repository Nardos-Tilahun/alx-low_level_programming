#include "main.h"
/**
 *Print_rev - reverse print string
 *
 *@s: string parameter
 *
 *Return: anyvalue
 */

void print_rev(char *s);
{
	int max;
	int count1;
	int count = 0;
	
	while (count >= 0)
	{
		if (s[count] == '\0')
		{
			break;
		}
		max = count ++
		for (count1 = max; count1 <= 0; count1--)
		{
			_putchar(s[count]);
		}
	_putchar(s[count]);
	}
}
