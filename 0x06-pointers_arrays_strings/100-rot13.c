#include "main.h"

/**
 * rot13 - concatinate two string
 * @s: second character pointer parameter
 * Return: pointer character
 */
char *rot13(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		s[i]
	}
	return (s);
}
