#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: - accept the first operand
 * @b: - accept the second operand
 * Return: the product of a and b (a * b)
 */
int mul(int a, int b)
{
	return (a * b);
}
